---
import { Code } from '@astrojs/starlight/components'

interface Props {
  name: string
  id: number
}

let { name, id } = Astro.props
let code = `mas "${name}", id: ${id}`
---

<Code code={code} lang="ruby" title="Brewfile" />
